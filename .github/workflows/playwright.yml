name: Playwright Tests

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: 18
          
      - name: Install dependencies
        run: npm install
        
      - name: Install Playwright
        run: npx playwright install --with-deps
        
      - name: Run tests
        id: tests
        run: |
          npx playwright test Aries_ui_testing/tests/auth/aries-login.spec.js --reporter=html
          echo "Listing files after test:"
          ls -la
          find . -name "*.html" -o -name "*.png" -o -name "*.webm" | head -20
        continue-on-error: true
        
      - name: Upload Test Reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
            **/*.png
            **/*.webm
            **/*.html
          retention-days: 30
        
      - name: Teams Notification
        if: always()
        run: |
          if [ "${{ steps.tests.outcome }}" == "success" ]; then
            STATUS="✅ PASSED"
            COLOR="00FF00"
          else
            STATUS="❌ FAILED"
            COLOR="FF0000"
          fi
          
          curl -H "Content-Type: application/json" -d "{
            \"@type\": \"MessageCard\",
            \"@context\": \"https://schema.org/extensions\",
            \"summary\": \"Playwright Test Result\",
            \"themeColor\": \"$COLOR\",
            \"sections\": [{
              \"activityTitle\": \"$STATUS Playwright Test\",
              \"activitySubtitle\": \"Repository: ${{ github.repository }}\",
              \"facts\": [{
                \"name\": \"Branch\",
                \"value\": \"${{ github.ref_name }}\"
              }, {
                \"name\": \"Author\",
                \"value\": \"${{ github.actor }}\"
              }, {
                \"name\": \"Test File\",
                \"value\": \"aries-login.spec.js\"
              }],
              \"potentialAction\": [{
                \"@type\": \"OpenUri\",
                \"name\": \"View Run\",
                \"targets\": [{
                  \"os\": \"default\",
                  \"uri\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
                }]
              }, {
                \"@type\": \"OpenUri\",
                \"name\": \"Download Reports\",
                \"targets\": [{
                  \"os\": \"default\",
                  \"uri\": \"${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts\"
                }]
              }]
            }]
          }" ${{ secrets.TEAMS_WEBHOOK_URL }}
